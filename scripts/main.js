/**
 * @fileoverview
 * Provides the JavaScript interactions for all pages.
 *
 * @author 
 * pjl
 */

/** namespace. */
var pjl = pjl || {};

/* Main */
pjl.main = function () {
	console.log("Ready");
};

pjl.main();
